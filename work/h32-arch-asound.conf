# Internal TV audio: automatic conversion to the verified vendor PCM format.
pcm.!default {
    type plug
    slave { pcm "hw:0,0" channels 1 rate 48000 format S16_LE }
}
ctl.!default { type hw card 0 }
